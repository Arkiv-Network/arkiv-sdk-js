# NPM Configuration for @golem-network scope

# Set the registry for the @golem-network scope
@arkiv-network:registry=https://registry.npmjs.org/

# Set access level for scoped packages (public by default)
@arkiv-network:access=public

# Optional: Set your npm username (if you want to be explicit)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
